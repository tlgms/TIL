<!-- notion-page-id: 3d32cdd741ac8012a36cdd2823221b8a -->

# 자기 참조 상한

정렬이나 비교가 필요한 API에서 관용적으로 쓰이는 패턴이다.

```kotlin
fun <T : Comparable<T>> maxOf(items: List<T>): T? = items.maxOrNull()
```

`T`가는 자기 자신과 비교 가능한 타입이어야 한다는 뜻이다.

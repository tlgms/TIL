<!-- notion-page-id: 3d02cdd741ac8012929cc5a2e5c3d951 -->

# 교차 타입

# **교차 타입(Intersection Types)**

교차 타입은 여러 타입을 하나로 결합한다. 기존 타입들을 합쳐서 필요한 기능을 모두 가진 단일 타입을 얻을 수 있다.

```typescript
interface ErrorHandling {
	success: beelean;
  	error?: {
    	message: string;
    }
}

interface UsersData {
	users: {
    	name: string;
    }
}

interface PostsData {
	posts: {
    	title: string;
    }
}

type UsersResponse = UsersData & ErrorHandling;
type PostsResponse = PostsData & ErrorHandling;

const handleUsersResponse = (response: UsersResponse) => {
	if(response.error) {
    	console.error('error : ', response.error);
      	return;
    }
  
  console.log('users : ', response.users);
}
```

## 응용

인라인 타입을 응용하여 속성의 타입을 그 자리에서 지정할 수도 있다.

```typescript
interface AuthRequest extends Request {
  user: User;
}
```

이렇게

```typescript
type AuthRequest = Request & { user: User };
```
